# 05 坐标与矩阵

## 学习目标

- 理解裁剪空间、模型空间与变换矩阵的关系。
- 使用 `mat3` 组合 2D 平移、旋转、缩放。
- 把“变换”当作可复用的渲染状态，而不是散落的坐标改写。

## 架构视角

矩阵是图形系统里的组合协议。对前端架构师来说，它像中间件链：每一步变换都不直接改业务对象，而是生成一个新的状态，最终由 shader 统一消费。

## 练习

- 修改 `scaleX` / `scaleY`，观察非等比缩放。
- 调整 `translation`，理解平移发生在裁剪空间。
- 尝试先平移再旋转，比较视觉结果。
